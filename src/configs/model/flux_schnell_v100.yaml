_target_: src.models.flux_model.FluxModel

repo_id: black-forest-labs/FLUX.1-schnell
# FP16 is faster on V100, but large classifier-amplified steering can
# overflow it. Set SHIFT_MODEL_DTYPE=float32 for stable high-strength runs.
dtype: ${oc.env:SHIFT_MODEL_DTYPE,float16}

memory:
  strategy: model_cpu_offload
  enable_vae_slicing: true
  enable_vae_tiling: true

load:
  revision: 741f7c3ce8b383c54771c7003378a50191e9efe9
  require_pinned_revision: true
  use_safetensors: true
  local_files_only: true
